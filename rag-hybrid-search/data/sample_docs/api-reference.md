# API Reference

The API service listens on port 8000 by default.

## Endpoints
- POST /v1/ask returns an answer with citations and confidence scores.
- GET /v1/documents lists indexed documents.
- POST /v1/ingest accepts a new document for indexing.
